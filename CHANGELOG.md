# 更新记录

本项目从 `v2026.09.05` 开始使用日期版本号，并通过带注释的 Git 标签记录可部署版本。

## v2026.09.17.6 - 2026-09-17

- 窄屏下语言控件默认只显示地球图标，点击后向左展开“Language”选择栏，选择语言后自动收回。
- 在语言控件左侧新增皮肤选择器，当前提供“默认风格”，并保留后续扩展多皮肤的状态与存储接口。
- 英文页面中皮肤选项同步显示为“Default Style”，桌面端保持皮肤与语言选择栏同时可见。
- 新增手机展开/收回、默认皮肤、控件顺序、语言及皮肤持久化的浏览器回归测试。

## v2026.09.17.5 - 2026-09-17

- 语言选择栏的原生选项精简为“中文”和“English”，不再包含“Language”占位项。
- “Language”改为覆盖在选择栏表面的固定文字；点击该区域时展开底层原生语言选择器。
- 新增双选项、覆盖文字和透明原生选择器的浏览器回归测试。

## v2026.09.17.4 - 2026-09-17

- 加宽右上角语言选择器，确保手机端完整显示“Language”，并同步调整导航可滚动区域避免重叠。
- 顶部导航下方的强调线改为更淡的半透明蓝色，并延伸至语言选择器下方、覆盖整个屏幕宽度。
- 新增语言选择器最小宽度、导航线颜色和覆盖范围的浏览器回归测试。

## v2026.09.17.3 - 2026-09-17

- 语言选择器收起时继续显示“Language”，展开后仅显示可选择的“中文”和“English”。
- “Language”占位项设置为隐藏且不可选择，并新增相应浏览器回归测试。

## v2026.09.17.2 - 2026-09-17

- “选择源文件”改为默认收起，与格式转换和信号 CSV 保持一致的紧凑初始状态。
- 右上角语言控件恢复为下拉选择器，控件恒定显示“Language”，可选择中文或 English；首次访问仍默认中文。
- “格式与兼容范围”更名为“格式与兼容范围说明”，并增加独立的琥珀色强调样式。
- 新增默认折叠、语言选择器占位文字与选项、兼容说明名称和配色的浏览器回归测试。

## v2026.09.17.1 - 2026-09-17

- 中文页面的语言按钮显示“English”，英文页面显示“中文”，点击即可直接切换并继续保留跨页面语言偏好。
- “转换为 CSV 文件”和“格式与兼容范围”默认收起，兼容范围可按需展开或再次收起。
- 手机端为语言按钮预留独立区域，导航链接可横向滚动且不再被按钮遮挡。
- 新增语言按钮文案、默认折叠状态及手机导航避让的浏览器回归测试。

## v2026.09.17 - 2026-09-17

- 顶部栏目导航最右侧新增中文 / English 语言选择器，首次访问默认中文。
- 语言选择保存在浏览器中，切换页面后继续使用已选语言，并可随时切回中文。
- CAN 报文解析、格式转换、关于本站三张页面的静态内容、辅助属性及运行时状态支持英文显示。
- 新增三页完整翻译、动态文件状态、跨页面持久化、切回中文及英文移动端布局回归测试。

## v2026.09.16.3 - 2026-09-16

- 01、02、03 恢复青色、绿色、紫色三种独立强调色。
- “文本编码与时间设置”移动到 01 的文件选择框下方，便于在选择源文件时就近配置。
- 03 标题改为“转换为 CSV 文件”，“已选 0 个”在 DBC 成功加载前保持隐藏。
- 顶部说明改为三步流程文字，并移除 ASC、LOG、TRC、BLF、TXT、MF4、MDF、CSV 格式标签框。
- 新增颜色、设置位置、CSV 计数显示时机和顶部精简的浏览器回归测试。

## v2026.09.16.2 - 2026-09-16

- “选择源文件”改为默认展开且可手动收起的折叠栏目，三个步骤统一使用相同交互结构。
- 02 改为绿色强调，03 改为与 01 一致的青色强调；展开提示在各标题栏右侧对齐。
- 三个栏目之间增加 8 px 深色间隔和上下边线，并补充默认状态、颜色、对齐及分隔回归测试。

## v2026.09.16.1 - 2026-09-16

- 为“选择源文件”“日志格式转换”“DBC 信号转换为 CSV”三个标题行分别增加青色、紫色、绿色视觉强调。
- 折叠栏目明确显示“点击展开 / 点击收起”，并保持桌面端与 iPhone 窄屏布局清晰可用。
- 新增标题颜色区分和折叠操作提示的浏览器回归测试。

## v2026.09.16 - 2026-09-16

- 将原始日志转换和 DBC 信号 CSV 改为两个横向折叠栏目，进入页面时默认折叠，点击标题可反复展开或收起。
- 移除“源文件格式”手动选择，所有日志直接按文件内容和扩展名自动识别。
- 源文件提示明确列出 ASC、LOG、TRC、BLF、TXT、MF4、MDF 七种格式，并继续支持混合格式多选。
- 七种目标格式改为可点击卡片，默认选中 ASC，当前格式通过蓝色边框和背景明确标识。
- 新增两个折叠栏、默认状态、格式卡片切换及移动端布局回归测试。

## v2026.09.14.3 - 2026-09-14

- 格式转换页调整为三行流程：源文件、原始日志目标格式与转换、DBC 信号 CSV。
- 原始日志转换支持一次选择多个文件及混合选择不同格式，逐个识别后统一输出为所选目标格式。
- 输入格式与目标格式一致的文件自动忽略，并在批次结果中显示转换及忽略数量。
- DBC 信号选择改为与 CAN 报文解析页一致的“可选信号 / 已选信号”双栏结构，并按报文分组。
- CSV 行按选择 DBC、选择信号、确定间隔、开始转换的顺序展示；多个日志分别生成独立 CSV。
- 新增批次规划、混合格式、多文件下载、同格式跳过与三行响应式布局回归测试。

## v2026.09.14.2 - 2026-09-14

- 顶部栏目导航改为粘性定位，页面向下滚动时始终停留在屏幕顶部。
- 移除当前栏目文字下方的白色长条，保留蓝色当前页状态。
- 格式转换页新增 DBC 信号 CSV：加载 DBC、搜索/选择信号，并提供 100–1000 ms 十档时间间隔。
- CSV 列按序号、时间、信号依次排列，固定采样点使用此前最近有效值，未出现的信号留空。
- DBC CSV 解码支持 Intel/Motorola、带符号、factor/offset、基础复用及 Float32/Float64，继续复用七种 CAN 日志输入。
- 新增解码数值、全部时间间隔、CSV 下载、滚动固定导航及无下划线回归测试。

## v2026.09.14.1 - 2026-09-14

- 关于本站和格式转换页的顶部导航改为固定展开；CAN 报文解析页初始固定，点击「生成曲线」后才自动收回并播放聚拢亮光。
- 格式转换表单从左右两列改为上下两行：先选择日志文件，再选择输出格式。
- 支持的桌面浏览器会请求保存位置并默认从源文件所在目录开始，输出保留源文件名主体；转换完成后自动写入。
- 无原路径权限、用户未确认位置、目录不可用或写入失败时改为显示下载，iPhone Safari 自动使用下载流程。
- 新增固定导航、生成曲线触发收回、原目录保存、同名下载与写入失败回退测试。

## v2026.09.13.6 - 2026-09-13

- 新增 `/convert` 本地 CAN 日志格式转换页面，三个页面的「格式转换」导航入口已接通，复用现有导航动画。
- 默认输出 ASC，提供 ASC / LOG / TRC / BLF / TXT / MF4 / MDF 七种读写选项；支持原始帧、远程帧和适用目标的 CAN FD。
- 提供格式自动识别、编码与 TXT 时间单位设置、首帧归零、进度、取消、预览及下载。
- 明确展示格式版本、原始 MDF 记录要求、精度损失与不支持记录的错误提示。
- 先添加独立格式样本与回归测试，再实现转换；增加 python-can/asammdf 交叉读取和浏览器七格式下载测试。修正原浏览器绘图测试未等待分批渲染完成的时序竞争。

## v2026.09.13.5 - 2026-09-13

### 页面

- 所有现有页面进入时默认展开顶部栏目导航，1 秒后以缓动动画收回，同时播放向中间聚拢的亮光。
- 栏目导航支持鼠标移入或点击展开，并在鼠标移出、点击页面其他位置或按下 Escape 时收回。
- 关于本站页面移除顶部“返回分析工具”和正文“开始分析 CAN 报文”按钮，保留导航中的“CAN报文解析”入口。
- 两个页面共用导航样式与交互脚本，避免后续行为不一致。

### 测试

- 新增导航启动时序、亮光动画、鼠标、触摸、焦点与键盘关闭行为的自动化状态测试。
- 扩展浏览器回归测试，覆盖两个页面的导航生命周期与关于本站按钮移除结果。

## v2026.09.13.4 - 2026-09-13

### 页面

- 主页面“关于本站”栏目现在跳转到 `/aboutus`。
- 关于本站页面复用主页面的收缩栏目导航，并将“CAN报文解析”链接回主页面。
- 未开放栏目的点击提示在两个页面中保持一致。

### 测试

- 新增主页面与关于本站页面双向导航、当前栏目及未开放栏目语义的回归测试。

## v2026.09.13.3 - 2026-09-13

### 页面

- 新增 `/aboutus` 关于本站页面，介绍 DBC/ASC 本地解析、信号曲线、报文统计、异常提示以及多设备适配能力。
- 页面下方新增微信与支付宝个人收款二维码卡片，目前保留正方形图片占位区，方便后续直接替换二维码图片。
- 页面使用与 CANAnalysis 主工具一致的深色视觉风格，并针对 iPhone 调整为单列响应式布局。

### 测试

- 新增关于本站页面、功能介绍、双二维码预留位及返回分析工具入口的回归测试。

## v2026.09.13.2 - 2026-09-13

### 功能

- “报文分析”新增“报文统计 → 1. 帧间隔（时间差）统计”，按当前 ASC 通道与方向中的 CAN ID 分行展示报文名称、总帧数、平均帧间隔、最小帧间隔和最大帧间隔。
- 帧间隔统一以毫秒显示；仅出现一帧的报文没有可计算间隔，明确显示“—”。
- 统计表支持横向滚动，便于在 iPhone 上查看完整列。

### 测试

- 新增按 CAN ID 分组、平均/最小/最大帧间隔计算、单帧边界及分析表格字段回归测试。

## v2026.09.13.1 - 2026-09-13

### 修复

- iPhone 生成曲线后自动收起信号选择区，并压缩文件栏与曲线工具栏；横屏时进一步隐藏非必要标题信息，为曲线保留可用高度。
- 移动端曲线工具栏支持横向滑动，缩短风格和坐标轴自适应按钮文案，确保右侧圆点与全屏控件可以触达。
- 移动端限制信号名称栏的初始宽度，并将侧栏分隔条改为更宽的触摸热区，支持手指拖动调整曲线与信号栏的宽度比例。
- 移动端增加当前曲线 Y 轴最小值和最大值输入；点选任意曲线后即可手动设置其纵轴范围，原有 X 轴范围输入保持可用。
- 使用动态视口高度，减少 iPhone Safari 地址栏变化造成的曲线区域挤压。

### 测试

- 新增移动端紧凑模式、信号区自动收起、工具栏横向滚动、Y 轴手动范围与分隔条触摸热区回归测试。

## v2026.09.13 - 2026-09-13

### 修复

- 大型 ASC 改为 2 MB 分片读取、逐行解析并分块压缩数据，不再同时保留完整文件缓冲区和完整文本副本，降低 iPhone 读取 200 MB 以上文件时的内存峰值。
- ASC 文件输入同时兼容 `input` 与 `change` 事件，选中文件后立即显示文件名和读取状态；取消选择时保留此前已加载的数据。
- 取消 DBC/ASC 文件输入的系统扩展名过滤，解决 iPhone 文件选择器中点选自定义扩展名文件后无法返回页面的问题；文件内容仍由解析器校验。
- 保留 DBC 中合法的零缩放因子，使此类信号始终按 offset 输出恒定物理值。
- 在解码前校验信号位是否完整落在 ASC 实际帧 DLC 内，避免缺失字节被静默补零并绘制成错误波形。
- 无有效信号的 DBC 不再显示为加载成功；清理旧选择、曲线和分析状态，文件选择或解析取消时则保留此前成功加载的数据。

### 测试

- 新增 ASC 跨文件切片行拼接、数据完整性及紧凑数组回归测试。
- 新增 iPhone 文件选择兼容性回归测试。
- 新增零缩放因子、帧长度不足及无有效信号 DBC 的浏览器回归测试。

## v2026.09.09.4 - 2026-09-09

### 修复

- 左上角图标继续使用仓库内的 `public/icon.png`，并增加图片预加载、高加载优先级和同步解码。
- 图标元素补充固定的 38×38 px HTML 宽高属性，确保样式加载和图片解码前后占位一致，避免刷新时跳动。

### 测试

- 新增图标本地路径、预加载策略、固定属性尺寸和实际占位尺寸回归测试。

## v2026.09.09.3 - 2026-09-09

### 修复

- 左上角提交号改为优先读取同源 `version.json`，不再依赖容易触发限流的 GitHub 公共提交接口；接口不可用时仍保持可见。
- 页面初始打开或刷新时，“已选信号”区域显示“请先加载DBC文件”。
- 顶部收缩导航的启动亮色改为从左右两侧向中心汇合，并在 1 秒内消退。

### 测试

- 新增初始提交号格式、已选信号默认提示以及左右聚拢动画名称和时长回归测试。

## v2026.09.09.2 - 2026-09-09

### 修复

- 左上角“CAN报文解析”、GitHub 提交码和部署时间区域固定为 132×46 px，三行分别使用固定行高。
- 提交码或部署时间异步写入时不再改变头部宽高，也不会推动右侧文件加载区域。
- GitHub 提交码与部署时间启用等宽数字显示，避免数字变化造成行内轻微抖动。

### 测试

- 新增空内容到完整提交信息更新前后的区域尺寸、右侧起始坐标和头部高度一致性测试。

## v2026.09.09.1 - 2026-09-09

### 修正

- 启动高亮动画改为作用于顶部 12 px 横条的整个背景色：由亮色在 1 秒内过渡到原有深色。
- “栏目导航”文字保持原有字体颜色，不再执行颜色或发光动画。

### 测试

- 回归测试改为分别校验横条背景动画和文字无动画状态，防止动画对象再次混淆。

## v2026.09.09 - 2026-09-09

### 交互

- 页面启动时，顶部“栏目导航”文字先以亮色显示，并在 1 秒内平滑过渡到常规颜色。
- 点击任一未开放栏目时，在鼠标点击位置旁显示“页面还未完成开发，不急不急～”，提示自动淡出且不会弹出窗口。
- 占位栏目支持连续点击重新播放提示，并自动避让浏览器右侧和底部边缘。

### 测试

- 新增启动高亮时长、占位栏目点击提示内容和鼠标旁定位回归测试。

## v2026.09.08 - 2026-09-08

### 功能

- 页面顶部新增悬停/键盘聚焦展开的栏目导航，默认以 12 px 窄栏收缩显示，不挤压主页面内容。
- 栏目顺序为：首页、CAN报文解析、格式转换、27930报文分析、J939分析、友情链接、关于本站。
- “CAN报文解析”标记为当前栏目；其余栏目仅作占位展示，暂不创建跳转链接。
- 窄屏下支持横向浏览全部栏目，并兼容减少动态效果的系统设置。

### 测试

- 新增导航栏目顺序、当前页、无跳转链接、禁用语义和默认收缩高度回归测试。

## v2026.09.07 - 2026-09-07

### 性能

- DBC/ASC 改为按字符位置增量扫描，不再一次性拆分整份文件，降低大文件加载时的内存峰值。
- 解析、ASC TypedArray 整理、曲线数据提取和统计计算统一使用约 8 ms 的自适应时间片，持续给动画、取消按钮和浏览器交互让出主线程。
- 大文件文本解码改为分片执行，并在开始读取与解析前等待首帧绘制，避免点击加载后界面短暂无响应。
- 信号列表先在离屏文档片段中构建后一次挂载，搜索输入增加防抖，窗口与拖拽引起的图表尺寸更新合并到每个动画帧一次。
- 长文件解析时显示实时百分比，提升等待过程的反馈连续性。

### 测试

- 新增 40,000 帧 ASC 响应性回归测试，确认解析期间主线程会被主动让出且报文、TypedArray 结果保持完整。

## v2026.09.06 - 2026-09-06

### 修复

- 左上角发布时间优先显示 `main` 对应的最新成功 Cloudflare Workers Build 完成时间。
- GitHub 部署检查不可用时回退到 `public/index.html` 的提交时间，并在悬停提示中标明时间来源。

### 测试

- 新增部署记录筛选回归测试，确保忽略失败构建和无关检查，并选择最新一次成功部署。

## v2026.09.05 - 2026-09-05

### 修复

- ASC 数据按“通道 + Rx/Tx”隔离，避免相同 CAN ID 在不同数据源之间混合。
- 跳过 RTR 等非数据帧、数据字节不足的帧以及同一 ID 中 DLC 变化的帧，不再用零静默补齐。
- 支持标准 DBC multiplex 定义（`M`、`m0`、`m1` 等），只解码当前复用值对应的分支。
- 加载新 ASC、无效 ASC 或切换数据源时清除旧曲线和旧分析，防止显示过期结果。
- 信号选择变化后使旧分析失效，要求重新生成曲线。
- 对 DBC 报文名、单位及枚举文本进行 HTML 转义，阻止文件内容注入页面。
- 超过 JavaScript 精确整数范围的 54–64 位信号不再静默绘制错误数值，而是明确标记为暂不支持。

### 性能

- ECharts 曲线启用 LTTB 降采样。
- 丢帧分析最多抽样 100,000 个间隔估算典型周期，并复用同一报文的计算结果。
- 分析面板复用曲线生成阶段已经计算的信号统计结果。

### 测试

- 新增浏览器回归测试，覆盖示例数据、无效 ASC 状态、通道隔离、RTR/DLC 校验、multiplex、DBC 注入及宽信号保护。

## 基线

- `baseline-pre-audit-20260905`：上述修复前的原始版本，提交 `d9477f3`。
